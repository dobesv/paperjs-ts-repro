import * as paper from 'paper';

const line: paper.Path = new paper.Path();

line.fillColor = 'rgb(255,255,255)';
line.strokeColor = 'rgb(255,255,255)';

line.fillColor = null;
line.strokeColor = null;
